# Independent Python Environment

Create this environment from the `healthcare-capacity` folder:

```bash
cd /Users/qiankangwang/Desktop/personal-portfolio/demos/healthcare-capacity
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
```

This project already has its own Python requirements and synthetic-data workflow. Keep it independent from the graduate-statistics and computational-statistics environments.
